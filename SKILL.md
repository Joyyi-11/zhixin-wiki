---
name: zhixin-wiki
description: |
  搭建和维护 Zhixin（知新）编译型个人知识库。适用于用户想用纯文件、Markdown 和 AI agent 把个人实践、外部材料和想法编译成主题判断，而不是只收藏和检索资料。触发场景包括：初始化知识库、入库外部材料、整理个人文章或想法、往回织主题页、显性处理反例和矛盾、基于编译层问答、生成选题或回顾知识库状态。
---

# Zhixin（知新）

Zhixin 是一套以「编译大于检索」为核心的个人知识库方法论与骨架。它把个人实践、外部材料和想法写成单源笔记，再往回织进主题页，形成可复用的当前判断。

## 核心原则

- **编译 > 检索**：问答和选题优先使用主题页、实体页和索引，不把每次回答都变成临时 RAG。
- **个人基准优先**：先用 `personal_basis` 确定问题与实践基准，再用 `curated_influence` 解释认知来源，最后用 `external_input` 补充、修正或反驳。
- **往回织不往后堆**：新材料必须回到已有主题、实体和判断关系中，不制造孤岛笔记。
- **反确认偏误**：每条材料标注支持、补充、修正、反驳或无关；主题页必须包含「挑战视角」。
- **捕获先于决策**：想法先落库，处置判断在共振触发或注意力快照时再做，不在捕获时过早决策。
- **原料只读**：`raw/` 和 `articles/` 是事实依据，后续不覆盖、不改写。
- **机械归脚本，语义归 agent**：索引、校验、回顾、语义健康检查交给脚本；摘要、判断、往回织交给 agent。

## 目录模型

```text
raw/           外部原文，只读事实依据
articles/      已发表或已完成的个人文章，只读事实依据
notes/         单源笔记，含 source_type 与 knowledge_role；own_thought 也在此
thoughts/      旧版想法暂存，兼容保留；新想法走共享捕获层（见下）
wiki/topics/   跨笔记形成的当前综合判断
wiki/entities/ 核心对象的当前认识
briefs/        经确认后保留的候选选题
INDEX.md       脚本生成的索引
```

跨项目共享捕获层（可选）：用户可配置一个 Workspace 根的单文件 Inbox（如 `inbox.md`，格式自由、只追加），所有项目「值得留」的想法统一写到这里，再晋升为 `notes/` 下的 `own_thought` 笔记。单机使用时可直接用 `thoughts/` 或免捕获层。

## 入库流程

收到链接、文章或外部材料时：

1. 读取完整正文或完整原始材料；抓取不完整时明确报告。
2. 把外部原文保存到 `raw/`；个人文章保存到 `articles/`。
3. 写单源笔记：摘要、要点、标签、`source_type`、`knowledge_role`、材料性质。
4. 读取 `INDEX.md`，定位候选主题页、实体页和相关笔记。
5. 判断新材料是在支持、补充、修正、反驳还是无关。
6. 必要时更新主题页或实体页，保留矛盾、边界和待验证问题。
7. 运行 `python scripts/build_index.py` 和 `python scripts/validate.py`。

外部发现源（如 AI HOT 类工具）只负责发现信息，不是知识库原料层：查询走公开只读接口，维护游标与去重，先按用户个人相关性过滤，给每条材料标注性质（支持 / 补充 / 修正 / 反驳 / 无关），显性区分支持类与反驳类。**被筛选出来不等于入库**；只有用户明确确认后，才读取并核验完整原文，进入链接入库流程。

## 三类知识角色

| 角色 | 含义 |
|---|---|
| `personal_basis` | 个人实践、想法和已完成表达，是主题判断的基准 |
| `curated_influence` | 主动整理并影响认知的外部内容 |
| `external_input` | 后续进入知识库、等待比较、补充、修正或反驳的材料 |

`source_type` 只说明来源渠道，例如 `web`、`own_article`、`own_thought`；它不能替代 `knowledge_role`。

## 想法笔记流程

想法是个人基准的一部分，不是外部资料：`source_type` 用 `own_thought`，`knowledge_role` 为 `personal_basis`。

- 想法没有外部原料，`source_path` 可空；写下来的文字本身就是知识，不得因缺少来源就放弃记录。
- 可短到一句话；先捕捉，后续在往回织时再补充背景、判断和与其他材料的关系。
- 落库 / 晋升时写入 `notes/YYYY-MM-DD-标题.md`，frontmatter 至少包含：`title`、`source_type`、`knowledge_role`、`source_project`（来源项目，用固定白名单）、`source_path`（可空）、`date`、`tags`、`entities`、`disposition`。
- `disposition` 取值：`待定`（默认）/ `执行` / `沉淀` / `放弃` / `讨论`。
- **处置决策节点**：不在捕获时决策。两个节点——① 共振触发（即时）：用户再提类似想法或触发主动回溯时，AI 弹出旧片段并给 disposition 建议，用户当场拍板；② 注意力快照（批量）：运行 `python scripts/review_attention.py` 把一批相关 `待定` 碎片聚簇，逐簇给建议，用户确认后批量更新。
- 不落库：纯待办执行事项、与既有笔记重复且无新增量、纯情绪碎屑。

## 注意力回溯（常驻规则）

会话内常驻操作规则，非后台任务：用户再问 / 再提时，AI 先对想法存储做一次共振扫描（按关键词 / 标签 / 实体，范围限定为共享捕获层与 `own_thought` 笔记及相关主题页、实体页，不扫描代码、规则文件等非想法文件）。

- 命中条件（防噪音）：找到 ≥2 条相关旧片段；或找到 1 条与当前想法构成矛盾 / 可延伸的强相关材料。
- 命中后，正式回答前附一段简短 inline 附言：「你 X 月 X 日在 X 也提过类似的…… 综合这几条，可以…… —— 建议 disposition：沉淀 / 执行 / 讨论」。
- 若构成矛盾，按反确认偏误机制显性呈现：区分高信度、有争议、开放问题。
- 依赖已落库想法；共享捕获层与 `own_thought` 笔记未建立时，先跑通捕获 / 晋升，再启用回溯。

## 主题页要求

主题页不是摘要拼接。它至少应包含：

- 当前结论
- 个人基准
- 重要认知来源
- 外部输入
- 材料如何互相影响
- 边界与矛盾
- 挑战视角
- 待验证问题
- 来源

只有至少两篇笔记形成可复用共同判断时，才新建主题页。

## 取用流程

- **问答流**：读 `INDEX.md` -> 主题页 / 实体页 -> 必要时回到笔记和原料。若相关主题页的「边界与矛盾」「挑战视角」或「待验证问题」与当前问题相关，必须在回答中说明；区分高信度判断（多源支持、无重大反例）、有争议判断（存在反例但倾向某方）、开放问题（矛盾未解决）。
- **选题流**：从主题页的矛盾、边界和待验证问题生成候选；新选题必须相对已发表内容产生认知增量；只在用户确认后写入 `briefs/`。
- **回顾流**：运行 `python scripts/review.py` 查看全库状态和该推进的主题，不自动改写判断。
- **语义健康检查**：运行 `python scripts/lint.py` 检查孤儿笔记、缺页实体、标签漂移、过期主题页和主题间潜在矛盾；建议每月或批量入库后运行。

## 安全边界

- 不用摘要冒充全文。
- 不把外部观点包装成用户个人实践。
- 不把未来由知识库生成的文章反向作为独立证据强化同一判断。
- 不自动入库外部发现结果；候选材料需用户确认。
- 处置判断不自动执行；`disposition` 变更需用户确认。
- 不自动删除、移动或覆盖原料层文件。
